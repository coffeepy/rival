{
	"name": "rival",
	"version": "0.1.0",
	"type": "module",
	"scripts": {
		"test": "bun test",
		"test:phase1": "bun test/phase1.test.ts",
		"check": "bun run check:types && bun run check:lint",
		"check:types": "tsc --noEmit",
		"check:lint": "biome check .",
		"format": "biome format --write .",
		"lint": "biome lint .",
		"lint:fix": "biome lint --write .",
		"ci": "bun run check && bun run test"
	},
	"dependencies": {
		"rivetkit": "^2.0.27",
		"zod": "^3.24.0"
	},
	"devDependencies": {
		"@biomejs/biome": "^1.9.0",
		"@types/bun": "latest",
		"typescript": "~5.9.3"
	}
}
